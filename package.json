{
  "name": "crawler",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run index.ts",
    "server": "bun run server.ts",
    "ui:dev": "cd ui && bun run dev",
    "ui:build": "cd ui && bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/jsdom": "^27.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "jsdom": "^27.2.0"
  }
}
